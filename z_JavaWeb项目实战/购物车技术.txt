一个图书购买界面    ShowBook

一个图书处理界面，对图书的数量，价格以及图书姓名 作处理  存放在hashMap中  BookCL

一个最终的购物车显示界面，用于显示界面2中的数据信息  ShoppingCar 
                                                                               （LinkedHashMap）可以用来顺序显示购买图书的信息

一个图书类，有名称，id号，价格，以及初始数量    Book

一个创建的数据库类，用于将书籍的信息全部加载在图书购买界面    DB


以上操作要求将数据进行适当的存储，如map集合 , list集合

要求：
	当用户点击购买商品时，把商品存放在session中，该session的结构是
	name      value
	myBook		hashMap对象
	
	而ashmap的结构是
	key       value
	书号		书对象
	
	
 
总结所需要用到的相关知识
	（1）Java基础的集合   ArrayList   HashMap
	（2）session技术存储值
	（3）servlet技术跳转界面
	（4）LinkedHashMap  使得存放的值不能相同，并且存放时有序
	（5）如何选择不同的集合
	   



分页技术
pageNow   当前页  
pageSize    一页中显示多少条数据
pageCount  一共有多少页
rowCount    共有多少记录，从数据库中查询

pageCount = (rowCount-1)  / pageSize+1;

1、SQL语句
oracle 分页技术
(select * form (select * from users order by id) where rownum<pageSize*pageNow) where rownum>=pageSize*((pageNow-1)+1);

2、算出共有数据
A： 查询出数据库表一共有多少页
       SQL语句
String sql = "select count(*) from users";
rs = stmt.executeQuery(sql);
rs.next( ); //将光标向下移，否则光标指向的是结果的最上行，也就是名字那一列
rowCount = rs. getInt( 1 ) ;

3、算出共有多少页
pageCount = rowCount%pageSize==0 ? rowCount/pageSize : rowCount/pageSize+1 ;
  

4、超链接显示页
 A: 遍历pageCount 得到多少个超链接
 B : 要想得到链接跳到每个页面，必须不断地更新SQL语句，也就是pageNow要不断的更新
    此时需要超链接将pageNow 返回给自己
     for (int i =1; i<=pageCount ; i++) {
       	out.println( " <a href ='/web应用名/Url路径？pageNow="+i+"  ' ><"+i+"></a>" );
      }
  

C: 先默认将pageNow 设置为1 , 接收超链接传递过来的pageNow
     int pageNow = 1 ; 

   String newPage = request.getParameter("pageNow");
   //注意将格式进行转换时，避免因为刷新未提交值而造成NumberFormatException 异常,也可以避免未点击超链接传值
      此时应该加入空值判断
    if( newPage != null) {
        pageNow = Integer.parseInt(newPage);
    }
 


5、显示上一页，以及下一页
  上一页：
   if(pageNow != 1) {
	out.println( " <a href ='/web应用名/Url路径？pageNow="+(pageNow-1)+"  ' >上一页</a>" );
   }
    
  下一页：
   if(pageNow != pageCount ) {
   	 out.println( " <a href ='/web应用名/Url路径？pageNow="+(pageNow+1)+"  ' >下一页</a>" );	
   }
   

6、显示当前页数以及总页数
 out.println("当前页'"+pageNow+"' / 总页数‘“+pageCount+”’   ")


7、输入框直接跳转到哪一页
跳转到 <input type = "text" name= "pageNow" /> <input type= "submit"  value="跳转" />

采用javaScript进行页面跳转

